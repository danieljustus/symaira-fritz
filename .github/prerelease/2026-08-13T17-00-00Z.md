<!-- prerelease: timestamp=2026-08-13T17:00:00Z repo=danieljustus/symaira-fritz head=2ebf71e7b4ffdd14ac8823941eb90014175605d9 target=v0.4.3 bump=patch gate=pass -->

# Prerelease Gate — 2026-08-13

## Summary
- Repo: danieljustus/symaira-fritz
- HEAD: 2ebf71e7b4ffdd14ac8823941eb90014175605d9
- Last release: v0.4.2
- Proposed target: v0.4.3 (bump: patch)
- Gate: pass

## Gate checks
| Check | Result | Detail |
|---|---|---|
| CI / tests (HEAD) | PASS | CI run 31716847237 and CodeQL run 31716846067 completed successfully for HEAD 2ebf71e (2026-08-13). `make lint`, `make build`, `make test` pass locally; fresh `go test ./... -cover` green. |
| Version consistency | PASS | `cmd/symfritz/main.go:22` version = `0.4.3`; `release-notes.md` prepared for v0.4.3 comparing `v0.4.2...v0.4.3`; README install commands use `@latest`. |
| README / doc currency | PASS | README describes the current interface; no user-facing feature changed since the 2026-08-10 gate (only release-workflow hardening #105/#106/#109 and docs #103/#107 landed). |
| Coverage vs threshold | PASS | lines 80.5% / threshold 80% (default, no hard repository gate found); carried forward from the 2026-08-10 measurement — no production `.go` diff since (only `main.go` version string). |
| Coverage regression | PASS | baseline 79.9% → 80.5% (delta +0.6 pp from 2026-08-10 measurement); no regression. |
| Patch coverage | PASS | 100.0% of instrumented production statements changed since v0.4.2 covered (2026-08-10 measurement); unreleased diff adds no new Go logic. |
| Assets preview | PASS | GoReleaser creates six platform archives plus `checksums.txt`. Darwin binaries are signed and notarized by `scripts/sign-and-notarize.sh` (API-key mode, `notarytool submit --wait`, only `Accepted` passes) before archives/checksums are created; any missing credential or non-`Accepted` status fails the release before publication (PR #109). |
| Distribution publishers | PASS | `Formula/symfritz.rb` has one GoReleaser `brews` writer at `.goreleaser.yml` (`directory: Formula`, token `HOMEBREW_TAP_GITHUB_TOKEN`); the secret exists in the `release` environment. No Cask is expected for this CLI-only repository. |
| Security | PASS | Dependabot, code-scanning, and secret-scanning open-alert counts are all zero (fresh query 2026-08-13). |

## Findings
- None blocking. The former BLOCK from 2026-08-10 (notarization invocation mixing Apple-ID/password flags with API-key secrets, issue #105) is resolved: `.github/workflows/release.yml` now passes `NOTARY_API_KEY`/`NOTARY_API_KEY_ID`/`NOTARY_API_ISSUER` through to `scripts/sign-and-notarize.sh`, which writes the `.p8` to a temp file and calls `notarytool submit --key --key-id --issuer --wait` (PR #106), and PR #109 added the fail-closed credential gate before GoReleaser.

## Applied mechanical fixes (default mode)
- None needed: version files already carried v0.4.3 from PR #107.

## Next step
- gate=pass → `/07-gh-release`
